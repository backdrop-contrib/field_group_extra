<?php

/**
 * @file
 * Test file for Field Group Extra.
 */

/**
 * Provides common functionality for the Field Group Extra test classes.
 */
class GroupExtraTestCase extends BackdropWebTestCase {
  /**
   * @var User
   */
  protected $admin_user;

  /**
   * @var string
   */
  protected $group_label;

  /**
   * @var string
   */
  protected $group_name;

  /**
   * @var string
   */
  protected $group_name_input;

  public function setUp() {
    $modules[] = 'node';
    $modules[] = 'field_ui';
    $modules[] = 'field_test';
    $modules[] = 'field_group';
    $modules[] = 'field_group_extra';
    $modules[] = 'field_group_tests';
    parent::setUp($modules);

    // Create test user.
    $this->admin_user = $this->backdropCreateUser(array('access content', 'administer content types', 'administer fields'));
    $this->backdropLogin($this->admin_user);
  }

}
